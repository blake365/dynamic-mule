1. 
2. 
3. 
    
4. add edit link to index view if logged in as admin
5. edit show page so missing elements are hidden
        if project.type = residential, don't show location info
        forEach through images to populate grid
6. make discreet login link


10. build dashboard